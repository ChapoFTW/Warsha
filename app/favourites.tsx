import MaterialIcons from '@expo/vector-icons/MaterialIcons'; import { FlatList,StyleSheet,View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { ProviderListItem } from '@/components/warsha/ProviderListItem'; import { ScreenHeader } from '@/components/warsha/ScreenHeader'; import { AppText } from '@/components/warsha/Typography'; import { spacing, typography, type ThemeColors } from '@/constants/theme'; import { useThemeColors, useThemedStyles } from '@/src/appearance/appearance-context'; import { useLocalPreferences } from '@/src/data/local-preferences'; import { useMarketplaceData } from '@/src/data/marketplace-context'; import { useLocalization } from '@/src/i18n/localization';
export default function FavouritesScreen(){
  const colors = useThemeColors();
  const styles = useThemedStyles(makeStyles);const{t}=useLocalization();const{providers}=useMarketplaceData();const{favouriteIds}=useLocalPreferences();const favourites=providers.filter((provider)=>favouriteIds.includes(provider.id));return <SafeAreaView style={styles.safe}><FlatList data={favourites} keyExtractor={(item)=>item.id} renderItem={({item})=><ProviderListItem provider={item}/>} ItemSeparatorComponent={()=><View style={{height:spacing.md}}/>} contentContainerStyle={styles.content} ListHeaderComponent={<View style={styles.header}><ScreenHeader title={t('favourites')} subtitle={`${favourites.length} ${t('savedProfessionals')}`}/></View>} ListEmptyComponent={<View style={styles.empty}><MaterialIcons name="favorite-border" size={38} color={colors.textMuted}/><AppText style={styles.title}>{t('noFavourites')}</AppText><AppText style={styles.body}>{t('favouritesHint')}</AppText></View>}/></SafeAreaView>}
const makeStyles = (colors: ThemeColors) => StyleSheet.create({safe:{flex:1,backgroundColor:colors.background},content:{padding:spacing.lg,paddingBottom:spacing.xxxl,maxWidth:720,width:'100%',alignSelf:'center'},header:{marginBottom:spacing.lg},empty:{alignItems:'center',padding:spacing.xxxl,gap:spacing.md},title:{fontSize:18,fontWeight:typography.semibold},body:{fontSize:13,color:colors.textMuted,textAlign:'center'}});

