import MaterialIcons from '@expo/vector-icons/MaterialIcons'; import { FlatList,StyleSheet,View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { ProviderListItem } from '@/components/warsha/ProviderListItem'; import { ScreenHeader } from '@/components/warsha/ScreenHeader'; import { AppText } from '@/components/warsha/Typography'; import { colors,spacing,typography } from '@/constants/theme'; import { useLocalPreferences } from '@/src/data/local-preferences'; import { useMarketplaceData } from '@/src/data/marketplace-context';
export default function FavouritesScreen(){const{providers}=useMarketplaceData();const{favouriteIds}=useLocalPreferences();const favourites=providers.filter((provider)=>favouriteIds.includes(provider.id));return <SafeAreaView style={styles.safe}><FlatList data={favourites} keyExtractor={(item)=>item.id} renderItem={({item})=><ProviderListItem provider={item}/>} ItemSeparatorComponent={()=><View style={{height:spacing.md}}/>} contentContainerStyle={styles.content} ListHeaderComponent={<View style={styles.header}><ScreenHeader title="Favourites" subtitle={`${favourites.length} saved professionals`}/></View>} ListEmptyComponent={<View style={styles.empty}><MaterialIcons name="favorite-border" size={38} color={colors.textMuted}/><AppText style={styles.title}>No favourites yet</AppText><AppText style={styles.body}>Tap the heart on a professional to save them here.</AppText></View>}/></SafeAreaView>}
const styles=StyleSheet.create({safe:{flex:1,backgroundColor:colors.background},content:{padding:spacing.lg,paddingBottom:spacing.xxxl,maxWidth:720,width:'100%',alignSelf:'center'},header:{marginBottom:spacing.lg},empty:{alignItems:'center',padding:spacing.xxxl,gap:spacing.md},title:{fontSize:18,fontWeight:typography.semibold},body:{fontSize:13,color:colors.textMuted,textAlign:'center'}});
