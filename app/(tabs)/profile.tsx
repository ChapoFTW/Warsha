import { router } from 'expo-router';
import { useEffect,useState } from 'react';
import { ActivityIndicator,Pressable,ScrollView,StyleSheet,TextInput,View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { AppText } from '@/components/warsha/Typography';
import { colors,radii,spacing,typography } from '@/constants/theme';
import { useAuth } from '@/src/auth/auth-context';
import { authMessageKey } from '@/src/auth/auth-errors';
import { dataErrorKey,logDataError } from '@/src/data/data-errors';
import { useLocalization } from '@/src/i18n/localization';
import { supabaseCustomerProfileRepository } from '@/src/repositories/supabase-user-repositories';

export default function Profile(){const{t,isRTL,language}=useLocalization();const auth=useAuth();const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[preferred,setPreferred]=useState<'en'|'ar'>(language);const[register,setRegister]=useState(false);const[editing,setEditing]=useState(false);const[busy,setBusy]=useState(false);const[message,setMessage]=useState('');useEffect(()=>{let active=true;if(auth.mode==='supabase'&&auth.user)void supabaseCustomerProfileRepository.get(auth.user.email??'').then(p=>{if(active){setName(p.displayName);setPreferred(p.preferredLanguage)}}).catch(e=>{logDataError('profile',e);if(active)setMessage(t(dataErrorKey(e)))});return()=>{active=false}},[auth.mode,auth.user,t]);const login=async()=>{setBusy(true);setMessage('');try{if(register){const result=await auth.signUp(name,email,password,'customer',preferred);if(result.needsEmailConfirmation)setMessage(t('checkEmail'))}else await auth.signIn(email,password)}catch(e){setMessage(t(authMessageKey(e)))}finally{setBusy(false)}};const save=async()=>{setBusy(true);try{await supabaseCustomerProfileRepository.update({displayName:name.trim(),preferredLanguage:preferred});setEditing(false)}catch(e){logDataError('profile update',e);setMessage(t(dataErrorKey(e)))}finally{setBusy(false)}};if(auth.loading)return <Page><ActivityIndicator color={colors.white}/></Page>;if(auth.mode==='mock'||auth.user)return <Page><AppText style={styles.title}>{auth.mode==='mock'?'Warsha Demo':(name||auth.user?.email||t('profile'))}</AppText><AppText style={styles.muted}>{auth.user?.email??''}</AppText>{editing?<><Field label={t('fullName')} value={name} onChangeText={setName} rtl={isRTL}/><View style={styles.row}>{(['en','ar'] as const).map(value=><Pressable key={value} onPress={()=>setPreferred(value)} style={[styles.button,preferred===value&&styles.selected]}><AppText>{value.toUpperCase()}</AppText></Pressable>)}</View><Pressable disabled={busy||name.trim().length<2} onPress={()=>void save()} style={styles.primary}><AppText style={styles.dark}>{t('saveAddress')}</AppText></Pressable></>:auth.mode==='supabase'?<Pressable onPress={()=>setEditing(true)} style={styles.button}><AppText>{t('fullName')}</AppText></Pressable>:null}<Pressable onPress={()=>router.push('/favourites')} style={styles.button}><AppText>{t('favourites')}</AppText></Pressable>{auth.mode==='supabase'?<Pressable onPress={()=>void auth.signOut()} style={styles.button}><AppText>{t('signOut')}</AppText></Pressable>:null}{message?<AppText style={styles.error}>{message}</AppText>:null}</Page>;return <Page><AppText style={styles.title}>{register?t('signUp'):t('signIn')}</AppText>{register?<Field label={t('fullName')} value={name} onChangeText={setName} rtl={isRTL}/>:null}<Field label={t('email')} value={email} onChangeText={setEmail} rtl={isRTL}/><Field label={t('password')} value={password} onChangeText={setPassword} secureTextEntry rtl={isRTL}/>{message?<AppText style={styles.error}>{message}</AppText>:null}<Pressable disabled={busy||!email||password.length<6} onPress={()=>void login()} style={styles.primary}>{busy?<ActivityIndicator color={colors.background}/>:<AppText style={styles.dark}>{register?t('signUp'):t('signIn')}</AppText>}</Pressable><Pressable onPress={()=>setRegister(v=>!v)} style={styles.button}><AppText>{register?t('signIn'):t('signUp')}</AppText></Pressable></Page>}
function Page({children}:{children:React.ReactNode}){return <SafeAreaView style={styles.safe}><ScrollView contentContainerStyle={styles.page}>{children}</ScrollView></SafeAreaView>}function Field({label,rtl,...props}:{label:string;rtl:boolean}&React.ComponentProps<typeof TextInput>){return <TextInput {...props} accessibilityLabel={label} placeholder={label} placeholderTextColor={colors.textMuted} style={[styles.input,{textAlign:rtl?'right':'left'}]}/>}const styles=StyleSheet.create({safe:{flex:1,backgroundColor:colors.background},page:{flexGrow:1,alignItems:'center',justifyContent:'center',padding:spacing.xl,gap:spacing.md},title:{fontSize:28,fontWeight:typography.bold,textAlign:'center'},muted:{color:colors.textMuted},input:{width:'100%',maxWidth:520,height:54,borderWidth:1,borderColor:colors.border,borderRadius:radii.lg,backgroundColor:colors.surface,color:colors.white,paddingHorizontal:spacing.lg},button:{minWidth:180,minHeight:50,borderWidth:1,borderColor:colors.border,borderRadius:radii.lg,alignItems:'center',justifyContent:'center',paddingHorizontal:spacing.lg},primary:{minWidth:180,minHeight:52,borderRadius:radii.lg,backgroundColor:colors.white,alignItems:'center',justifyContent:'center'},dark:{color:colors.background,fontWeight:typography.bold},row:{flexDirection:'row',gap:spacing.sm},selected:{borderColor:colors.white,backgroundColor:colors.surfaceElevated},error:{color:colors.error}});
