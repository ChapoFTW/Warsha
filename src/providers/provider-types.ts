export type ProviderOnboardingStatus='draft'|'submitted'|'pending_review'|'more_information_required'|'approved'|'rejected'|'suspended';
export type ProviderPricingModel='fixed'|'starting'|'hourly'|'inspection'|'quote';
export type ProviderServiceInput={serviceId:string;name:string;price:number;pricingModel:ProviderPricingModel;transportationFee:number;emergencySurcharge:number};
export type ProviderAreaInput={governorate:string;district:string;radiusKm:number};
export type ProviderDraft={id?:string;status:ProviderOnboardingStatus;displayName:string;avatarUrl:string;profession:string;about:string;experienceYears:number;ratingAverage?:number;languages:string[];skills:string[];categoryIds:string[];services:ProviderServiceInput[];areas:ProviderAreaInput[];serviceRadiusKm:number;isAvailable:boolean;emergencyAvailable:boolean;temporaryUnavailableUntil?:string;agreementAccepted:boolean};
export const emptyProviderDraft:ProviderDraft={status:'draft',displayName:'',avatarUrl:'',profession:'',about:'',experienceYears:0,ratingAverage:0,languages:[],skills:[],categoryIds:[],services:[],areas:[],serviceRadiusKm:15,isAvailable:false,emergencyAvailable:false,agreementAccepted:false};
export function providerCompletion(value:ProviderDraft){const checks=[value.displayName,value.profession,value.about,value.experienceYears>0,value.languages.length,value.skills.length,value.categoryIds.length,value.services.length,value.areas.length,value.agreementAccepted];return Math.round(checks.filter(Boolean).length/checks.length*100)}
