import { createContext,PropsWithChildren,useCallback,useContext,useEffect,useMemo,useRef,useState } from 'react'; import { localBookingRepository } from './booking-repository'; import type { Booking,BookingStatus,CancellationReason,NewBooking } from './booking-types';
export type { Booking,BookingStatus,BookingStatusHistory,BookingAttachment,Address,TimeSlot,PriceBreakdown,CancellationReason,NewBooking } from './booking-types';
type Value={bookings:Booking[];loading:boolean;error:string|null;creating:boolean;actionInFlight:string|null;createBooking:(input:NewBooking)=>Promise<Booking>;cancelBooking:(id:string,reason?:CancellationReason)=>Promise<void>;rescheduleBooking:(id:string,date:string,time:string)=>Promise<void>;simulateStatus:(id:string,status:BookingStatus)=>Promise<void>;getBooking:(id:string)=>Booking|undefined;reload:()=>Promise<void>};
const Context=createContext<Value|null>(null);
export function BookingProvider({children}:PropsWithChildren){const[bookings,setBookings]=useState<Booking[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const[creating,setCreating]=useState(false);const[actionInFlight,setActionInFlight]=useState<string|null>(null);const locks=useRef(new Set<string>());const submitLock=useRef(false);const reload=useCallback(async()=>{setLoading(true);try{setBookings(await localBookingRepository.list());setError(null)}catch(reason){setError(reason instanceof Error?reason.message:'Unable to load bookings')}finally{setLoading(false)}},[]);useEffect(()=>{void reload()},[reload]);const update=useCallback((booking:Booking)=>setBookings((current)=>[booking,...current.filter((item)=>item.id!==booking.id)]),[]);const runAction=useCallback(async(key:string,operation:()=>Promise<Booking>)=>{if(locks.current.has(key))return;locks.current.add(key);setActionInFlight(key);try{update(await operation())}finally{locks.current.delete(key);setActionInFlight((current)=>current===key?null:current)}},[update]);const value=useMemo<Value>(()=>({bookings,loading,error,creating,actionInFlight,reload,getBooking:(id)=>bookings.find((item)=>item.id===id),createBooking:async(input)=>{if(submitLock.current)throw new Error('Booking submission is already in progress');submitLock.current=true;setCreating(true);try{const booking=await localBookingRepository.create(input);update(booking);return booking}finally{submitLock.current=false;setCreating(false)}},cancelBooking:async(id,reason='other')=>runAction(`${id}:mutation`,()=>localBookingRepository.cancel(id,reason)),rescheduleBooking:async(id,date,time)=>runAction(`${id}:mutation`,()=>localBookingRepository.reschedule(id,date,time)),simulateStatus:async(id,status)=>runAction(`${id}:mutation`,()=>localBookingRepository.updateStatus(id,status,'Development simulation'))}),[actionInFlight,bookings,creating,error,loading,reload,runAction,update]);return <Context.Provider value={value}>{children}</Context.Provider>}
export function useBookings(){const context=useContext(Context);if(!context)throw new Error('useBookings must be used inside BookingProvider');return context}
