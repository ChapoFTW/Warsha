# Warsha Specification Authority Index

## Authority order

1. Warsha Constitution
2. Locked WPS documents
3. WES documents
4. Existing implementation, migrations, tests, operations, and experiments

When implementation conflicts with a locked specification, the conflict must be reported and corrected through a forward change. When an as-built WPS records historic behavior later corrected by a locked WPS, the later locked specification governs. Silence in a higher authority does not permit a lower authority to invent material product behavior.

## Document register

| Document | Version | Status | Authority relationship | Implementation state | Migration state | Manual-test state |
| --- | --- | --- | --- | --- | --- | --- |
| `docs/constitution/Warsha-Constitution.md` | 1.0 | **LOCKED** | Highest authority | Repository alignment audited against it | No standalone migration; implemented through forward WPS migrations | Constitutional checklist review performed for alignment; no independent manual suite |
| `docs/wps/WPS-001-foundation-authentication.md` | 1.0 | **AS-BUILT BASELINE - SUBJECT TO CONSTITUTION** | Records implementation; later locked corrections govern | Expo Router, isolated data modes, customer email/password, worker phone OTP foundation, session/recovery implemented; deletion deferred | Existing migrations `202607200001`-`202607200006`, `202607200009`, alignment `202607310001`; all local | No dedicated manual result set; OTP delivery blocked locally without SMS provider |
| `docs/wps/WPS-002-customer-experience.md` | 1.0 | **AS-BUILT BASELINE - SUBJECT TO CONSTITUTION** | Records customer baseline; WPS-007/008 corrections govern | Home, categories, discovery, profiles, direct booking compatibility, orders, detail, notifications, payments/reviews integrations implemented | Existing customer/realtime/review/trust/financial/alignment migrations; all local | No dedicated signed manual result set |
| `docs/wps/WPS-003-independent-worker-experience.md` | 1.0 | **AS-BUILT BASELINE - SUBJECT TO CONSTITUTION** | Records worker baseline; Constitution and WPS-008 simplify/extend it | Worker onboarding/profile, binary availability, jobs, verification, earnings implemented; WPS-008 marketplace flows absent | Provider/jobs/verification/financial/alignment migrations; all local | No dedicated signed manual result set |
| `docs/wps/WPS-004-booking-lifecycle.md` | 1.0 | **AS-BUILT BASELINE - SUBJECT TO CONSTITUTION** | Authoritative record of existing booking graph; WPS-008 pre-booking lifecycle must convert into it | Creation, assignment, approval, reschedule, milestones, cancellation, history, attachments, chat, financial/review links implemented with listed gaps | Existing booking/jobs/chat/financial/alignment migrations; all local | Automated pgTAP present; no signed end-to-end manual results |
| `docs/wps/WPS-005-realtime-notifications.md` | 1.0 | **AS-BUILT BASELINE - SUBJECT TO CONSTITUTION** | Records existing invalidation/durable notification architecture | Central Realtime, customer/worker/detail subscriptions, notifications/read/dismiss/foreground/mock parity implemented; push absent | Realtime plus domain publication migrations; all local | No signed reconnect/background/push-disabled manual results |
| `docs/wps/WPS-006-trust-reviews-verification.md` | 1.0 | **AS-BUILT BASELINE - SUBJECT TO CONSTITUTION** | Records existing trust baseline; WPS-008 eligibility rules govern marketplace use | Reviews, rating summary, replies, attachments, identity verification, optional Skill Certificate, sanitized trust and discovery gating implemented | Review, participant RLS, verification, alignment migrations; all local | Automated pgTAP present; no signed production moderation/document manual results |
| `docs/wps/WPS-007-financial-system.md` | 1.0 | **LOCKED - LOCAL IMPLEMENTATION ALIGNED, LIVE PROVIDERS DISABLED** | Locked within financial scope; WPS-008 must reuse it | Ledger, snapshots, payments, cash debt, earnings, refunds, holds, recovery, provider-neutral adapters and UI implemented locally; live money disabled | `202607300001_payments_earnings_ledger.sql`, `202607300002_financial_spec_alignment.sql`; pending/local only | Runbook prepared; every recorded case remains **NOT RUN** |
| `docs/wps/WPS-008-marketplace-intelligence.md` | 1.2 | **LOCKED FOR IMPLEMENTATION** | Locked marketplace product authority below Constitution and integrated with WPS-007 | Request, invitation, quote, matching, Emergency, Rescue, scheduling, no-show, and capacity paths are implemented locally; production activation remains fail-closed | `202607310001_repository_alignment.sql`, `202607310002_marketplace_intelligence_schema.sql`, and `202607310003_marketplace_intelligence_api.sql`; pending hosted, clean-reset validated locally | 71 criteria pass automated evidence, 4 are approved fail-closed deferrals, and 5 remain validation-pending; manual suite **NOT RUN** |
| `docs/wes/WES-008-marketplace-intelligence.md` | 1.4 | **IMPLEMENTED LOCALLY - PRODUCTION ACTIVATION GATED** | Engineering detail subordinate to Constitution and WPS-001 through WPS-008 | Local schema/API, Expo repositories and flows, Realtime invalidation, Mock path, and automated tests implemented; operational and manual gates remain | Dedicated schema/API migrations `202607310002` and `202607310003`; pending hosted, 96 WPS-008 pgTAP assertions pass | Acceptance evidence is recorded; native E2E, fairness replay, accessibility/RTL device review, and manual suite remain open |
| `docs/wps/WPS-009-communication-collaboration.md` | 1.0 | **LOCKED — IMPLEMENTED LOCALLY, MANUAL ACCEPTANCE PENDING** | Locked booking-communication authority below Constitution; extends WPS-004/005 and reuses WPS-008 confirmation/Rescue/Running Late | Confirmed-booking inbox/chat, private image/PDF attachments, localized quick/system events, unread/typing, disabled relay, safety reports, reminder, and motto correction implemented locally | `202608010003_wps009_communication_collaboration.sql`; clean-reset validated locally, not applied hosted | 12 pgTAP files / 765 assertions and local client/build gates pass; 55-case manual alpha remains **NOT RUN**; linked comparison is transport-blocked |
| `docs/wes/WES-009-communication-collaboration.md` | 1.0 | **IMPLEMENTED LOCALLY — PRODUCTION PROVIDERS AND MANUAL ACCEPTANCE GATED** | Engineering detail subordinate to Constitution and WPS-009 | Existing chat/realtime/notification stack extended; no rebuild, dialer, live push, telephony, or retention scheduler | Forward migration `202608010003`; no hosted mutation | Local automated validation passes; linked ledger/dry-run transport-blocked; manual suite **NOT RUN** |

| `docs/wps/WPS-010-worker-profiles-portfolio.md` | 1.0 | **LOCKED — IMPLEMENTED LOCALLY, MANUAL ACCEPTANCE PENDING** | Locked worker-profile authority below Constitution; extends WPS-003/006/008 without replacing their worker, trust, review, or quote models | Private profile media, simple profile management, multi-image portfolio, relevant certificates, sanitized public profile, full discovery gating, and Mock parity implemented locally | `202608010004_wps010_worker_profiles_portfolio.sql`; clean-reset validated locally, not applied hosted | 13 pgTAP files / 842 assertions and client/build gates pass; 60-case manual alpha remains **NOT RUN**; linked comparison is transport-blocked |
| `docs/wes/WES-010-worker-profiles-portfolio.md` | 1.0 | **IMPLEMENTED LOCALLY — MANUAL AND HOSTED DEPLOYMENT GATED** | Engineering detail subordinate to Constitution and WPS-010 | Existing provider/catalog/verification/review architecture extended; no parallel identity, quote, or review system | Forward migration `202608010004`; no hosted mutation | Local automated/build validation passes; linked ledger/dry-run transport-blocked; manual suite **NOT RUN** |
| `docs/wps/WPS-011-reviews-reputation.md` | 1.0 | **LOCKED — IMPLEMENTED LOCALLY, MANUAL ACCEPTANCE PENDING** | Locked review/reputation authority below Constitution; extends WPS-004/005/006/008/010 without replacing their booking, notification, trust, ranking, or profile models | Verified multidimensional reviews, bounded edits, immutable replies, private photos, helpful voting, reporting, audited soft-hide moderation, deterministic reputation/badges/confidence, and Mock parity implemented locally | `202608010005_wps011_reviews_reputation.sql`; clean-reset validated locally, not applied hosted | 14 pgTAP files / 922 assertions and local client/build gates pass; 60-case manual alpha remains **NOT RUN**; linked comparison is transport-blocked |
| `docs/wes/WES-011-reviews-reputation.md` | 1.0 | **IMPLEMENTED LOCALLY — MANUAL AND HOSTED DEPLOYMENT GATED** | Engineering detail subordinate to Constitution and WPS-011 | Existing review, storage, provider projection, catalog, notification, Realtime, and Mock architecture extended; no parallel review system or ranking input | Forward migration `202608010005`; no hosted mutation | Local automated validation passes; manual suite **NOT RUN**; linked ledger/dry-run are transport-blocked |
| `docs/wps/WPS-012-job-execution-operations.md` | 1.0 | **LOCKED — IMPLEMENTED LOCALLY, MANUAL ACCEPTANCE PENDING** | Locked post-confirmation operational authority below Constitution; extends WPS-004/005/007/008/009/010/011 without replacing booking, money, communication, profile, or review systems | Fine-grained server states, immutable participant timeline, predefined updates/delays, private progress media, WPS-007 additional-work approval, checklists, customer completion, warranty, return sections, and Mock parity implemented locally | `202608010006_wps012_job_execution_operations.sql`; clean-reset validated locally, not applied hosted | 15 pgTAP files / 1,029 assertions pass; TypeScript, ESLint, Expo Doctor, and Android/iOS/Web exports pass; manual alpha remains **NOT RUN**; linked comparison blocked by transport |
| `docs/wes/WES-012-job-execution-operations.md` | 1.0 | **IMPLEMENTED LOCALLY — MANUAL AND HOSTED DEPLOYMENT GATED** | Engineering detail subordinate to Constitution and WPS-012 | Existing booking, chat, notification, financial, review, Realtime, Storage, and Mock architecture extended; no parallel booking or financial system | Forward migration `202608010006`; no hosted mutation | Local clean reset, 1,029 pgTAP assertions, client regressions, Expo Doctor, and three platform exports pass; linked/manual gates are recorded in WPS-012 evidence |
| `docs/wps/WPS-013-disputes-resolution.md` | 1.0 | **LOCKED — IMPLEMENTED LOCALLY, MANUAL ACCEPTANCE PENDING** | Locked exceptional-dispute authority below Constitution; extends WPS-004/005/007/008/009/011/012 without replacing booking, money, communication, review, or operations | Server eligibility, one active case, immutable evidence/actions, staff workflow, review hold, WPS-007 financial delegation, WPS-012 return visit, WPS-009 projection, private storage, and Mock parity implemented locally | `202608020001_wps013_disputes_resolution.sql`; clean-reset validated locally, not applied hosted | 16 pgTAP files / 1,123 assertions, 13 regression suites, TypeScript, ESLint, Expo Doctor, and Android/iOS/Web exports pass; 60-case manual alpha remains **NOT RUN**; linked comparison is transport-blocked |
| `docs/wes/WES-013-disputes-resolution.md` | 1.0 | **IMPLEMENTED LOCALLY — MANUAL AND HOSTED DEPLOYMENT GATED** | Engineering detail subordinate to Constitution and WPS-013 | Dormant dispute tables and existing booking evidence sources extended; no parallel financial, chat, review, return-visit, or ranking system | Forward migration `202608020001`; no hosted mutation | Clean reset, 1,123 pgTAP assertions, 182 WPS-013 contracts, complete client gates, Doctor 18/18, and all three exports pass; linked/manual gates remain open |
| `docs/wps/WPS-014-notifications-engagement.md` | 1.0 | **LOCKED — IMPLEMENTED LOCALLY, DATABASE/MANUAL ACCEPTANCE PENDING** | Locked unified-notification authority below Constitution; extends WPS-005 and consumes WPS-001 through WPS-013 sources without replacing them | Existing durable inbox extended with categories, priorities, grouping, source links, archive, counts, preferences, quiet-hours policy, typed routing, disabled push/reminders, and Mock parity | `202608020002_wps014_notifications_engagement.sql`; static/client validated locally, not applied hosted; clean reset blocked | 217 WPS-014 checks, 13 existing regression suites, TypeScript, ESLint, mojibake, Doctor 18/18, and three exports pass; 82 new pgTAP assertions and 60 manual cases remain unrun |
| `docs/wes/WES-014-notifications-engagement.md` | 1.0 | **ENGINEERING BASELINE — LOCAL CLIENT VALIDATION COMPLETE** | Engineering detail subordinate to Constitution and WPS-014 | Existing notification/Realtime repositories, context, banner, center, preferences, and domain producers extended; no parallel event system | Forward migration `202608020002`; no hosted mutation; local database execution blocked | Client/static/build gates pass; production push/scheduler, database execution, linked review, and manual acceptance remain gated |
| `docs/wps/WPS-017-operations-analytics-admin-platform.md` | 1.0 | **LOCKED — IMPLEMENTED LOCALLY, MANUAL ACCEPTANCE PENDING** | Locked staff-operations authority below Constitution; gates WPS-006 through WPS-016 without replacing any domain decision | Nine capability-based staff roles, eighteen work queues, a generic assignment layer over authoritative records, restricted safe search, sanitized account views, versioned configuration change control with dual control, server-authoritative feature flags, restrictive kill switches, activated support cases, manual incidents, a read-only audit explorer, privacy-safe analytics with minimum-cell suppression, and bounded exports implemented locally | `202608020005_wps017_operations_analytics_admin.sql`; clean-reset validated locally, not applied hosted | 21 pgTAP files / 1,831 assertions pass including a 308-assertion WPS-017 suite (updated by WPS-018 to present server-verified session claims); TypeScript, ESLint, mojibake, Expo Doctor, and Android/iOS/Web exports pass; 66-case manual alpha remains **NOT RUN** |
| `docs/wes/WES-017-operations-analytics-admin-platform.md` | 1.0 | **ENGINEERING BASELINE — MANUAL AND HOSTED DEPLOYMENT GATED** | Engineering detail subordinate to Constitution, the domain WPS documents, and WPS-017 | Existing staff gate, notification, storage, catalog, and Mock architecture extended; no parallel dispute, enforcement, refund, moderation, or notification system | Forward migration `202608020005`; no hosted mutation | Local clean reset, 1,693 pgTAP assertions, 15 custom regression suites, Expo Doctor, and three platform exports pass; MFA, export file delivery, support attachments, and per-domain legacy gating remain deferred |
| `docs/wps/WPS-018-production-readiness-launch.md` | 1.0 | **LOCKED — IMPLEMENTED LOCALLY, LAUNCH BLOCKED** | Locked launch-readiness authority below Constitution; hardens WPS-006 through WPS-017 without introducing any customer feature | Server-verified session freshness replacing the WPS-017 client attestation, enforceable MFA via Supabase TOTP, capability gates on 22 legacy staff RPCs, dual control, session revocation, periodic access review, four-environment model with immutable history, 19 rate-limited surfaces, redacted structured observability, and a post-deployment verification gate implemented locally | `202608030001_wps018_production_readiness.sql`; clean-reset validated locally, not applied hosted | 21 pgTAP files / 1,831 assertions pass including a 136-assertion WPS-018 suite; 18 regression suites, 3 audit scripts, Expo Doctor 18/18, and Android/iOS/Web exports pass; 44-case manual alpha remains **NOT RUN**; private beta and production are both **NO-GO** |
| `docs/wes/WES-018-production-readiness-launch.md` | 1.0 | **ENGINEERING BASELINE — LAUNCH GATED** | Engineering detail subordinate to Constitution, the domain WPS documents, WPS-017, and WPS-018 | 30 functions hardened by renaming the original body into `private` unchanged and re-publishing a gate under the original public name; no domain logic rewritten | Forward migration `202608030001`; no hosted mutation | Local clean reset, 1,831 pgTAP assertions, 18 regression suites, and three exports pass; staging project, SMS provider, backup verification, load execution, and legal review remain open |
| `docs/wps/WPS-019-customer-support-help-center.md` | 1.0 | **LOCKED — IMPLEMENTED LOCALLY, MANUAL ACCEPTANCE PENDING** | Locked support and knowledge authority below Constitution; extends WPS-017 support cases and WPS-014 notifications without replacing the dispute (WPS-013), moderation (WPS-016), chat (WPS-009), or staff capability models | A Help Center with 12 categories and 29 bilingual articles, full-text plus bounded trigram search with minimum-cell suppressed popular queries, context-aware ordering across 15 surfaces, a reachable contact and ticket surface with server-decided reopen rules and satisfaction, private server-validated attachments, and a staff queue with service levels, macros, resolution reasons, and merge — all implemented locally | `202608040001_wps019_customer_support_help_center.sql`; clean-reset validated locally, not applied hosted | 22 pgTAP files / 2,032 assertions pass including a 201-assertion WPS-019 suite; 19 regression suites (858 WPS-019 checks), four audit scripts, TypeScript, ESLint, mojibake, Expo Doctor 18/18, and cache-cleared Android/iOS/Web exports pass; 58-case manual alpha remains **NOT RUN**; evidence in `docs/testing/WPS-019-ACCEPTANCE-EVIDENCE.md` |
| `docs/wes/WES-019-customer-support-help-center.md` | 1.0 | **ENGINEERING BASELINE — MANUAL AND HOSTED DEPLOYMENT GATED** | Engineering detail subordinate to Constitution, WPS-001 through WPS-018, and WPS-019 | Existing support tables, RPCs, notification catalog, staff capability, storage pattern, and rate limiter extended; the WPS-017 intake body reused verbatim through a no-default 8-argument overload; lifecycle applied by trigger so four WPS-017 RPCs gained it without a line rewritten | Forward migration `202608040001`; no hosted mutation | Local clean reset, 2,032 pgTAP assertions, and all client/build gates pass; the attachment picker, article editor, merge UI, and SLA breach emitter remain open and are recorded |
| `docs/wps/WPS-020-search-discovery-personalization-appearance.md` | 1.0 | **LOCKED — IMPLEMENTED LOCALLY, MANUAL ACCEPTANCE PENDING** | Locked search, discovery, personalization, and appearance authority below Constitution; subordinate to WPS-008 for ranking, WPS-010/011 for worker data, WPS-017/018 for analytics | Server-authoritative provider search with four explicit outcomes and bounded trigram tolerance, eleven server-enforced filters, five documented sorts whose Recommended applies the WPS-008 policy rather than inventing one, discovery shelves that each answer one question, bounded owner-isolated recent searches and recently viewed, and a full three-preference appearance system over 73 semantic tokens in two designed themes | `202608050001_wps020_search_discovery_personalization_appearance.sql`; clean-reset validated locally, not applied hosted | 23 pgTAP files / 2,170 assertions pass including a 138-assertion WPS-020 suite; 20 of 21 regression suites pass (872 WPS-020 checks) with one pre-existing `app.json` failure WPS-020 did not cause; five audit scripts, TypeScript, ESLint, mojibake, Expo Doctor 18/18, and cache-cleared Android/iOS/Web exports pass; 102-case manual alpha remains **NOT RUN**; evidence in `docs/testing/WPS-020-ACCEPTANCE-EVIDENCE.md` |
| `docs/wes/WES-020-search-discovery-personalization-appearance.md` | 1.0 | **ENGINEERING BASELINE — MANUAL AND HOSTED DEPLOYMENT GATED** | Engineering detail subordinate to Constitution, WPS-001 through WPS-019, and WPS-020 | Existing discoverability gate, catalog, favourites, ranking policy, rate limiter, and observability extended; colour moved from module-evaluation time to render time across 72 files by style factories whose parameter shadows the former import, so ~900 references stayed byte-identical | Forward migration `202608050001`; no hosted mutation | Local clean reset, 2,170 pgTAP assertions, and all client/build gates pass; device location, response-time sorting, a light native launch screen, and measured on-device contrast remain open and are recorded |

| `docs/wps/WPS-021-growth-referrals-promotions.md` | 1.1 | **LOCKED — IMPLEMENTED LOCALLY, DISABLED BY DEFAULT, MANUAL ACCEPTANCE PENDING** | Locked growth, referral, and promotion authority below Constitution; subordinate to WPS-007 for all money, WPS-008 for ranking, WPS-014 for notifications, WPS-016 for enforcement, and WPS-017/018 for staff capability, dual control, flags, limits, and observability | Customer and worker referral codes from a rejection-sampled 31-symbol alphabet, immutable one-per-account attribution, qualification only on an authoritative completed booking carrying a WPS-007 price snapshot, inert reward entitlements that are explicitly not a balance, staff-authored and dual-control-approved Warsha-funded campaigns, and a twelve-condition server-authoritative eligibility result that is the only way a promotion is ever visible | `202608060001_wps021_growth_referrals_promotions.sql`; clean-reset validated locally, not applied hosted | 24 pgTAP files / 2,308 assertions pass including a 138-assertion WPS-021 suite; all 23 pre-existing suites pass with no assertion edited; 21 of 22 regression suites pass with one pre-existing `app.json` failure WPS-021 did not cause; five audit scripts, TypeScript, ESLint, mojibake, Expo Doctor 18/18, and cache-cleared Android/iOS/Web exports pass; 62-case manual alpha remains **NOT RUN**; evidence in `docs/testing/WPS-021-ACCEPTANCE-EVIDENCE.md` |
| `docs/wes/WES-021-growth-referrals-promotions.md` | 1.1 | **ENGINEERING BASELINE — MANUAL AND HOSTED DEPLOYMENT GATED** | Engineering detail subordinate to Constitution, WPS-001 through WPS-020, and WPS-021 | Existing price snapshot, ledger account, dual control, feature flags, kill switches, capability gate, staff audit, fraud sink, notification catalog, rate limiter, and observability extended; WPS-021 introduces no new infrastructure of any kind, and three allowlists that had no room for a `growth` member were honoured rather than widened | Forward migration `202608060001`; no hosted mutation | Local clean reset, 2,308 pgTAP assertions, and all client/build gates pass; entitlement fulfilment automation, deep-link invite association, a device-fingerprint signal, and a clipboard affordance remain open and are recorded |

| `docs/wps/WPS-022-privacy-data-lifecycle-user-rights.md` | 1.0 | **LOCKED — IMPLEMENTED LOCALLY, DISABLED BY DEFAULT, MANUAL ACCEPTANCE PENDING** | Locked privacy, data-lifecycle, and user-rights authority below Constitution; constrains what may be removed from every earlier WPS while rewriting none of them; subordinate to WPS-007 for money, WPS-009 for messages, WPS-011 for reviews, WPS-013/016 for evidence, and WPS-014/017/018 for notifications, capability, audit, flags, and limits | Twelve registered data classes and a machine-readable inventory; an immutable eight-purpose consent ledger backfilled from the two pre-existing scalar acknowledgements; deactivation and deletion as separate products; a nine-blocker server-authoritative deletion workflow where a blocked request stays cancellable and `outstanding_earnings` prevents a debt being cancelled in Warsha's favour; anonymization that removes the person and preserves every record another party depends on; an owner-scoped export whose manifest carries its own exclusion list; eleven retention rules behind a five-condition execution guard; and narrowly-scoped legal holds with a mandatory review date that their own author cannot release | `202608070001_wps022_privacy_data_lifecycle_user_rights.sql`; clean-reset validated locally, not applied hosted | 25 pgTAP files / 2,547 assertions pass including a 218-assertion WPS-022 suite; all 24 pre-existing suites pass with no assertion edited; five audit scripts, TypeScript, ESLint, mojibake, Expo Doctor, and cache-cleared Android/iOS/Web exports pass; 72-case manual alpha remains **NOT RUN**; evidence in `docs/testing/WPS-022-ACCEPTANCE-EVIDENCE.md` |
| `docs/wes/WES-022-privacy-data-lifecycle-user-rights.md` | 1.0 | **ENGINEERING BASELINE — MANUAL AND HOSTED DEPLOYMENT GATED** | Engineering detail subordinate to Constitution, WPS-001 through WPS-021, and WPS-022 | Existing capability gate, staff audit, sensitive-access log, rate limiter, operational log, feature flags, kill switches, dual control, notification catalog, environment helper, observability retention, soft-delete columns, and WPS-020 history clearing all extended; six allowlists honoured rather than widened; privacy configuration deliberately kept as a singleton table rather than a WPS-017 configuration domain, because that domain's payload validator covers a closed set of shapes | Forward migration `202608070001`; no hosted mutation | Local clean reset, 2,547 pgTAP assertions, and all client/build gates pass; export file generation, scheduled deletion execution, storage-object sweeping, and auth disablement remain open, are honestly disabled rather than half-built, and are recorded |
| `docs/wps/WPS-023-authentication-role-onboarding-worker-vetting.md` | 1.0 | **LOCKED — IMPLEMENTED LOCALLY, DISABLED BY DEFAULT, MANUAL ACCEPTANCE PENDING** | Locked authentication-entry, role-onboarding, and worker-vetting authority below Constitution; corrects how the application is entered and adds a vetting requirement that did not previously exist; subordinate to WPS-001 for authentication, WPS-006/010 for provider identity and verification, WPS-016 for trust, WPS-017 for staff capability, and WPS-022 for privacy | Authentication-first entry replacing an app that opened onto the customer home while signed out; an account model where every account is a customer and worker capability is an additional server-approved role; a fifteen-state server-authoritative worker lifecycle with a single private writer, row locking, idempotent transitions, and immutable history; twenty-four fail-closed activation gates; National ID capture with advisory-only quality warnings and hash-plus-last-four storage; a provider-neutral extraction boundary that can never approve anybody; Model A criminal-record submission in a dedicated private bucket behind a dedicated capability with no offence column in schema; a versioned vetting policy whose twelve factors are all reviewer judgement and whose legal review is pending; and SQL-enforced appeal-reviewer independence | `202608080001_wps023_authentication_role_onboarding_worker_vetting.sql`; clean-reset validated locally, not applied hosted | 26 pgTAP files / 2,707 assertions pass including a 160-assertion WPS-023 suite; all 25 pre-existing suites pass with no assertion edited; 525 WPS-023 client checks; five audit scripts, TypeScript, ESLint, mojibake, Expo Doctor 18/18, and cache-cleared Android/iOS/Web exports pass; 108-case manual alpha remains **NOT RUN**; evidence in `docs/testing/WPS-023-ACCEPTANCE-EVIDENCE.md` |
| `docs/wes/WES-023-authentication-role-onboarding-worker-vetting.md` | 1.0 | **ENGINEERING BASELINE — MANUAL AND HOSTED DEPLOYMENT GATED** | Engineering detail subordinate to Constitution, WPS-001 through WPS-022, and WPS-023 | Existing authentication, provider profile, verification workflow, identity store, address table, storage buckets, trust state, staff capability model, staff audit, sensitive-access log, notification catalogue, feature flags, kill switches, and WPS-022 privacy registers all extended rather than duplicated; three cases where WPS-023 was reaching into another specification's validated inventory were resolved by backing out rather than by editing the assertion | Forward migration `202608080001`; no hosted mutation | Local clean reset, 2,707 pgTAP assertions, and all client/build gates pass; a live camera overlay, a map provider, an extraction provider, the staff decision UI, the certificate binary upload, and computed quality heuristics remain open, are honestly disabled rather than half-built, and are recorded in WES-023 §9 |
| `docs/wps/WPS-024-LEGAL-PRIVACY-COMPLIANCE-GOVERNANCE.md` | 1.0 | **LOCKED — IMPLEMENTED LOCALLY, DISABLED BY DEFAULT, MANUAL ACCEPTANCE PENDING, LEGAL REVIEW PENDING** | Locked legal, privacy, compliance and governance authority below Constitution; supersedes the previously proposed WPS-024 (Manual QA), which becomes WPS-025; subordinate to WPS-022 for consent and privacy machinery and WPS-023 for verification and worker lifecycle | Twenty-six complete legal documents in English and Egyptian Arabic with no placeholder text; a versioned document register whose published versions are immutable in every column but `status`; an append-only acceptance ledger binding a named person to an exact text, language and instant through a SHA-256 chain spanning TypeScript, Node and Postgres; five change classes where material and urgent force re-consent and editorial and non-material deliberately do not; decline handling that records a decline as a decline, never lets inaction become consent, and preserves records, export, deletion, support, appeals and account closure; subprocessor, data-processing and data-retention registers where `approved_not_integrated` is a distinct published state from `in_use`; AI governance where training on identity data is barred by a CHECK constraint rather than a policy note; and the worker activation model moved from post-review to provisional-with-post-hoc-review without granting the system any power to make a decision | `202608090001`, `202608100001` and `202608110001`; clean-reset validated locally, not applied hosted | 27 pgTAP files / 2,902 assertions pass including a 195-assertion WPS-024 suite; all 26 pre-existing suites pass with no assertion edited; 659 WPS-024 client checks; five audit scripts, TypeScript, ESLint, mojibake, Expo Doctor 18/18, and cache-cleared Android/iOS/Web exports pass; 96-case manual governance suite remains **NOT RUN**; evidence in `docs/testing/WPS-024-ACCEPTANCE-EVIDENCE.md` |
| `docs/wes/WES-024-LEGAL-PRIVACY-COMPLIANCE-GOVERNANCE.md` | 1.0 | **ENGINEERING BASELINE — MANUAL, LEGAL AND HOSTED DEPLOYMENT GATED** | Engineering detail subordinate to Constitution, WPS-001 through WPS-023, and WPS-024 | Existing consent purposes, privacy inventory, retention rules, staff capability model, staff audit, notification catalogue, feature flags, kill switches, rate-limit policies and WPS-023 activation gates all extended rather than duplicated; the acceptance ledger sits alongside the WPS-022 consent ledger because the two answer different questions and neither subsumes the other; one case where WPS-024 reached into WPS-023’s validated signed-out surface was resolved by backing out rather than by editing the assertion | Forward migrations `202608090001`, `202608100001` and `202608110001`; no hosted mutation | Local clean reset, 2,902 pgTAP assertions, and all client/build gates pass; Google Cloud Vision, Google Maps Platform and Expo Camera are selected and registered but not integrated, the reduced review copy is a policy commitment without a pipeline, there is no publish control in the admin surface, re-consent enforcement ships off, and no lawful basis is confirmed — all recorded in WES-024 §9 |
| `docs/architecture/external-provider-registry.md` | 1.0 | **AUTHORITATIVE — TWO PROVIDERS IMPLEMENTED AND AWAITING CREDENTIALS** | Operational authority for every external dependency, subordinate to WPS-024; distinct from the published Subprocessor Register, which is a privacy disclosure | Seven providers registered with every required field plus a capability role, so business logic names no vendor; `provider_enabled()` as the single answer to whether a service may be called, requiring registry status, feature flag and kill switch to all agree; a CHECK constraint preventing a device-side provider from ever holding a server credential; and `implemented_awaiting_credential` as a distinct published state from `active`, so the register records what is true rather than what is built | `202608100001_wps024_provider_activation.sql` and `202608110001_wps024_provider_abstraction_health.sql`; clean-reset validated locally, not applied hosted | 195-assertion WPS-024 pgTAP suite and 659 client checks include the full provider surface; no credential appears in any of the three cache-cleared bundles; Google Cloud Vision and Google Maps Platform are implemented behind Edge Functions and remain disabled because no credential has been supplied |
| `docs/architecture/provider-abstraction.md` | 1.0 | **AUTHORITATIVE — CONTRACTS IMPLEMENTED, NO PROVIDER EXERCISED** | Design authority for how Warsha calls a replaceable external service, subordinate to WPS-024 and to the External Provider Registry | An `OcrProvider` interface with `extractDocument`, `extractIdentity`, `extractConfidence` and `extractMetadata`, and a `MapProvider` interface with `autocomplete`, `placeDetails`, `forwardGeocode`, `reverseGeocode` and `renderMap`, each with exactly one implementation resolved by the capability role the database records; Egyptian National ID field parsing kept provider-agnostic and separately versioned so accuracy changes can be attributed to the vendor or to the parser; timeouts and retry ceilings fixed on the contracts so every implementation inherits them; and a device-side renderer registry that keeps the address surface free of any mapping library | `202608110001_wps024_provider_abstraction_health.sql`; clean-reset validated locally, not applied hosted | pgTAP asserts no capability surface names a vendor and that a second live provider for a singular role raises `23505`; the regression suite asserts the extraction function, the location proxy, both contracts, the field parser and the address surface name none either, and that the renderer key agrees across both runtimes; **no provider has been called, so no interface has been exercised against a live vendor** |
| `docs/operations/google-cloud-setup-runbook.md` | 1.0 | **PROCEDURE — NOT EXECUTED** | Operational procedure for taking the two Google integrations from awaiting-credential to active, subordinate to WPS-024 | Project, billing and budget prerequisites; the four APIs Warsha actually calls and no others; a Vision service account scoped to one OAuth scope with no Storage access; the billed Places and Geocoding key held only in Edge Function secrets; the publishable render keys and their Android signing and iOS bundle restrictions, including the Play app-signing fingerprint that is the usual cause of a grey map in a store build; and the three-gate sequence for turning a provider on, ending in the material Privacy Policy change that adding a subprocessor requires | None | **Nothing in it has been executed**: no Google Cloud project, billing account, enabled API or issued key exists for Warsha; the regression suite asserts that `app.json` carries placeholder names and no string matching a Google API key shape |


## Migration safety boundary

- This index describes local repository state only.
- No hosted migration was applied while creating these baselines.
- The 2026-08-01 linked ledger and dry-run attempts failed before login-role initialization with a Supabase transport error. The candidate pending list is recorded relative to the last successfully verified hosted endpoint (`202607290002`); it is not represented as a fresh hosted observation.
- WPS-009 passed a fresh clean reset, 12 pgTAP files / 765 assertions, and all custom/client/export gates. No `db push` was executed.
- WPS-010 adds forward migration `202608010004`; it is not authorized for hosted application during implementation.
- WPS-010 passed a clean reset, 13 pgTAP files / 842 assertions, 11 custom regression commands, Expo Doctor 18/18, and separate cache-cleared Android/iOS/web exports. The linked ledger and dry-run failed before comparison with a transport error; no `db push` was executed.
- WPS-011 adds forward migration `202608010005`; it passed a clean reset, 14 pgTAP files / 922 assertions, all custom/client gates, Expo Doctor 18/18, and separate cache-cleared Android/iOS/web exports. Fresh linked-ledger and non-mutating dry-run attempts failed during login-role initialization with `LegacyDbConfigLoginRoleNetworkError` / `TransportError`. No hosted migration was executed; manual acceptance and fresh linked comparison remain gated.
- WPS-012 adds forward migration `202608010006`; it passed a clean reset and 15 pgTAP files / 1,029 assertions, plus TypeScript, ESLint, Expo Doctor, and Android/iOS/Web exports. It remains local-only; no hosted migration or deployment was executed. The linked checks were blocked by hosted transport and manual alpha remains **NOT RUN**, as recorded in `docs/testing/WPS-012-ACCEPTANCE-EVIDENCE.md`.
- WPS-013 adds forward migration `202608020001`; clean reset, 16 pgTAP files / 1,123 assertions, 13 custom regression suites, TypeScript, ESLint, Expo Doctor 18/18, and Android/iOS/Web exports pass. Linked ledger and dry-run attempts failed before login-role initialization with `LegacyDbConfigLoginRoleNetworkError` / `TransportError`; no hosted mutation occurred. Manual alpha remains **NOT RUN**.
- WPS-014 adds forward migration `202608020002`; it remains local-only. The first hosted push attempt failed on this file with `42601 syntax error at or near "from"`; because the file was recorded in neither the hosted nor any local ledger, it was repaired in place. Four defects were corrected: two schema-qualified `pg_catalog.extract(epoch from ...)` calls (special `EXTRACT` grammar is valid only unqualified, replaced with `pg_catalog.date_part('epoch', ...)`), and three PL/pgSQL variables shadowing table columns (`provider_id` in `refresh_notification_discoverability`, `request_id` in `notification_audience` and `resolve_notification_route`) which raised `42702` at trigger/RPC execution time. No remotely applied migration was edited.
- WPS-014 now passes a clean reset applying the full chain through `202608020002`, **17 pgTAP files / 1,205 assertions executed (`Result: PASS`)** including its own **82/82** suite, 14 custom regression suites, 217 focused checks, TypeScript, ESLint, mojibake, `git diff --check`, Expo Doctor 18/18, and cache-cleared Android/iOS/web exports. An executed database security audit confirmed 252/252 SECURITY DEFINER functions pinned to empty `search_path`, zero private-table exposure, owner-only RLS, `authenticated`-only RPC grants, private-schema Realtime exclusion, UUID-only payload allowlisting, and fail-closed push/scheduler flags; no grant, policy, or RLS rule was weakened.
- WPS-016 adds forward migration `202608020004`; it is local-only and becomes the single authority for platform trust state, unified abuse reporting, moderation, enforcement, bans, fraud signals, investigations, and appeals. It **extends and replaces nothing**: WPS-006 verification, WPS-009 chat abuse reports, WPS-011 review moderation, WPS-013 disputes, WPS-015 chargebacks, WPS-007 earning holds, and WPS-008 marketplace eligibility all keep their authority, and no existing table, RPC, trigger, or policy was modified or dropped.
- WPS-016 delivers eleven enforcement measures plus restoration, a unified immutable report intake spanning eight surfaces and seventeen categories, centralized server-authoritative trust state that clients cannot modify, ten advisory-only fraud signals, appeals with a mandatory decision note and explicit restoration, and an immutable actor/timestamp/reason/evidence moderation audit. **No automatic permanent bans** — a ban requires a human staff actor plus an investigated report, and a database constraint forbids a system actor from issuing anything except a non-punitive investigation. No external moderation provider and no AI moderation exist anywhere.
- WPS-015 adds forward migration `202608020003`; it is local-only and extends the WPS-007 financial system without creating a second ledger, a second payment state machine, or any new ledger transaction type. It delivers the provider-neutral production foundation: four-mode environment resolution (`disabled`/`mock`/`sandbox`/`live`) that fails closed, a provider registry and per-environment account binding, server-authoritative payment-method availability, secret **metadata** only, verified-webhook processing with signature/replay/environment/allowlist/amount/currency checks and quarantine, settlement import, daily reconciliation with a typed exception queue and explicit ledger balancing, chargeback intake requiring staff review, payout tokenization that fails closed, verified payout events, and a disabled six-hour release scheduler.
- WPS-015 provider selection is **DEFERRED** to a formal decision gate recorded in `docs/decisions/payment-provider-selection.md`. Research used official Paymob, Geidea, Kashier, and Meeza sources; every remaining unknown is commercial or legal (entity eligibility, commercial terms, marketplace disbursement licensing, worker national-ID processing, tax treatment, chargeback liability, partial-refund support, webhook signing scheme). No fee or settlement term was invented. Real payments, payouts, refunds, webhooks, schedulers, and hosted migrations remain **not executed**; 60 manual cases are **NOT RUN**.
- WPS-017 adds forward migration `202608020005`; it is local-only and becomes the single authority for staff identity, staff capabilities, operational assignment, operational configuration change control, feature flags, kill switches, support cases, incidents, the audit explorer, privacy-safe analytics, and approved exports. It **extends and replaces nothing** — WPS-006 verification, WPS-007/WPS-015 money, WPS-008 marketplace, WPS-011 review moderation, WPS-013 disputes, WPS-014 notifications, and WPS-016 enforcement all keep their authority, and no existing table, public function, policy, or grant was dropped or weakened.
- WPS-017 makes exactly two additive changes to earlier work, both documented in place: a fourth notification audience (`staff`) that is structurally invisible to customer and worker inboxes, and a **widening** of `private.is_staff()` so a WPS-017 grant no longer requires a legacy `user_roles` row. The legacy result is unchanged for every account that already had it; Support Agent, Marketplace Operations, and Security Administrator deliberately hold no legacy bridge capability and cannot reach any pre-WPS-017 domain staff RPC. The residual cross-domain gap inside the roles that do hold it is recorded in `docs/security/admin-threat-model.md` (T8).
- WPS-017 is fail-closed by construction: the admin surface requires an explicit build flag, production cannot be selected without an MFA requirement, no MFA provider is configured, every feature flag ships disabled, every kill switch ships inactive and can only restrict, and export file delivery does not exist. Real payments, payouts, refunds, push, SMS, calls, webhooks, schedulers, and external providers remain **not executed**; 60 manual cases are **NOT RUN**.
- WPS-018 adds forward migration `202608030001`; it is local-only and introduces **no customer feature**. It closes the three residual WPS-017 security findings: client-attested re-authentication is replaced by server-verified freshness read from the GoTrue-signed `amr` claim that PostgREST verifies; MFA becomes enforceable through Supabase TOTP with `aal2` checked per caller; and 22 pre-WPS-017 staff RPCs gain their specific domain capability gate. Each of those RPCs was **renamed into `private` unchanged** and re-published under its original public name behind the gate, so not one line of WPS-006 through WPS-016 logic was rewritten and no domain error code or message drifted.
- WPS-018 makes production structurally safe by constraint rather than by policy: production cannot be selected without the MFA requirement, cannot accept the pre-WPS-017 staff gate, and cannot disable dual control. It also adds session revocation checked on every capability, a 90-day access review that forbids self-review, dual control on permanent bans and refunds, a four-environment model with immutable change history, 19 audited rate-limited surfaces each naming where its limit actually lives, structured observability with redaction enforced at write time, and `verify_platform_release()` which deliberately reports an honest failure while a launch gate is open.
- WPS-018 records that **automated tests passing is not readiness**. `docs/launch/READINESS-GAP-REGISTER.md` closes 18 gaps and leaves 17 open. Private beta is **NO-GO** on eight blockers — 0 of 486 consolidated manual cases executed, no hosted staging project, no SMS provider, no verified backup or restore, no legal review, no company structure, nobody rostered, and zero device testing. Production is **NO-GO** on a further nine, including the deferred payment provider, unresolved payout licensing, no deployed webhook, no executed load test, and 16 accepted transitive dependency advisories. No hosted migration was applied and no production deployment was performed.
- WPS-014 read-only hosted checks were executed: `supabase migration list` shows everything through `202608020001` as local **and** remote, with `202608020002` local-only; `supabase db push --linked --dry-run` confirms it as the single pending migration. **No hosted mutation occurred.** The retry command `npx.cmd supabase db push --linked` is documented but was not executed. Production push, token activation, provider delivery, and scheduled processing remain disabled; 60 manual cases remain **NOT RUN**.
- WPS-019 adds forward migration `202608040001`; it is local-only and extends the existing support architecture rather than replacing it. `public.support_tickets`, `public.support_messages`, and `public.support_ticket_events` keep their identity and every WPS-017 support RPC keeps its name, signature, and body. The intake gained an eight-argument overload that declares no default values, so the four-argument WPS-018 wrapper stays unambiguous; `private` still holds exactly 30 preserved `_impl` functions.
- WPS-019 changed two existing assertions deliberately and recorded both: the notification category count moved from nine to ten, and a WPS-017 assertion that used an empty customer inbox as a proxy for "no staff notification leaks into a customer inbox" was rewritten to assert that property directly. Neither weakened a check.
- WPS-019 enables nothing: push delivery, token registration, the notification scheduler, every feature flag, and every kill switch remain off, asserted negatively in pgTAP.
- WPS-019 recovery found `npm run audit:secrets` failing on every run, broken by the WPS-018 commit `c88957a`: the scanner was flagging its own credential-shape pattern definitions and the security review that documents them. It was repaired without weakening detection — the PEM header is assembled from parts, the documented Hermes string is written as two halves, and two exact literals are neutralized by value alongside Supabase published demo keys. Neutralization is by value and never by path, because a path allowlist would let a real secret hide in that file permanently. The bundle scanner was then re-verified in both directions against planted credentials and against the supabase-js guard.
- The pending hosted chain after WPS-019 is two migrations: `202608030001` (WPS-018) and `202608040001` (WPS-019). Both are local-only; the linked dry run confirmed them without mutating the hosted project.
- WPS-020 adds forward migration `202608050001`; it is local-only and extends the existing architecture rather than replacing it. `private.is_provider_publicly_discoverable` stays the single discoverability authority and is called from every new read path without being restated; `get_marketplace_catalog()` is untouched; `public.favourites` remains the only saved-provider store; the WPS-008 `best-value-v1` policy remains the only ranking authority; and analytics and rate limiting reuse the WPS-018 authorities. All 22 pre-existing pgTAP suites pass with **no assertion edited**.
- WPS-020's governing finding was that neither search nor appearance existed as a system. Search was a client-side substring match over a full catalog download — correct only while the dataset is small, and wrong silently once it is not — and `recommended` was a local heuristic wearing the name of an authority it never called. Colour was resolved at module-evaluation time in 73 files, with `Colors.light` holding dark values, so anything trusting it rendered dark and looked correct.
- WPS-020 browse-time recommendation is an **application** of `best-value-v1`, not a new formula: it reproduces the published weights and reads the fairness and new-worker bounds from `private.marketplace_configuration` at call time, writes no `marketplace_candidate_scores` row, and starts no matching run. Two deviations are recorded in the function itself — no request means no capacity or ETA term, and with no location the distance term is absent and the remaining weights are deliberately **not** renormalized.
- WPS-020 refused two specified features rather than shipping them hollow. Response-time sorting is absent because `provider_profiles` stores a free-text label and no numeric value, so the control would have sorted by nothing; device location is absent because `expo-location` is not a dependency and a permission flow that cannot be tested without a device was judged worse than not shipping it. The server accepts and tests coordinates; the client does not ask. Both are recorded in WPS-020 §17 and WES-020 §8.
- WPS-020 changed `app.json` `userInterfaceStyle` from `dark` to `automatic`. This was mandatory: pinned to `dark`, iOS reports dark to the app regardless of the device setting, and the System appearance could never have worked. The native launch screen still stays dark in both appearances, because only a light-ink splash asset exists and producing a dark-ink one is the brand system's authority.
- WPS-020 validation found `npm run test:wps018` already failing, on an assertion that over-the-air updates are not enabled. The `"updates"` and `ios.infoPlist` blocks in `app.json` were uncommitted working-tree changes present before WPS-020 began; they were swept into the `9237b82` preservation commit alongside WPS-020's single `userInterfaceStyle` line, which the WPS-018 suite does not read. Neither the file nor the assertion was touched — the assertion is correct and is reporting a real change in launch posture that belongs in the WPS-018 gap register.
- The WPS-020 implementing session was interrupted mid-write on `hooks/use-color-scheme.ts`; the write did not land and the file was recovered unchanged. The recovering session completed it so the hook reports the resolved app appearance rather than the device setting, deleted the now-redundant `.web` variant that returned `'light'` until hydration, and re-ran every gate from the recovered tree. That closed the last hidden dark-mode assumption in the repository — one whose only consumers are Expo starter components no Warsha screen renders, so no screenshot would ever have caught it.
- The pending hosted chain after WPS-020 was three migrations: `202608030001` (WPS-018), `202608040001` (WPS-019), and `202608050001` (WPS-020). All three are local-only; the linked dry run confirmed them without mutating the hosted project.
- **Superseded during WPS-021.** An executed `supabase migration list --linked` on 2026-08-05 shows all three of those now applied remotely; the hosted project is current through `202608050001`. The statement above is retained as the record of what was true when WPS-020 closed.
- WPS-021 adds forward migration `202608060001`; it is local-only and introduces **no financial system**. A promotion reaches a customer price through exactly one route — `bookings.price_breakdown.discount` plus a reduced `final_price_egp`, read by the existing `private.create_booking_price_snapshot`, which computes `provider_gross_minor := customer_total_minor + promotion_minor` and posts `warsha_promotion_expense`. Worker gross, the commission basis, provider net, and payout eligibility are arithmetically incapable of being reduced by a promotion, and pgTAP asserts each one.
- WPS-021 introduces **no new infrastructure**. Dual control, feature flags, kill switches, staff capability, staff audit, fraud signals, notifications, rate limiting, and observability are all the existing WPS-014 through WPS-018 authorities. Three allowlists on already-applied migrations had no room for a `growth` member — `operational_log_category_check`, `staff_capabilities_domain_check`, and `trust_fraud_signals_key_check` — and each was honoured rather than widened, with growth abuse mapped onto the existing WPS-016 vocabulary so it lands in the queue staff already triage.
- WPS-021's governing product decision is that a referral reward is an **inert entitlement**, not a balance. The locked scope forbids a customer wallet, a credit balance, and booking credits; WPS-007 is the sole financial authority. An entitlement therefore carries no amount, cannot be transferred, cannot be summed, and posts no ledger row — it becomes real only when staff have already approved a campaign to honour it. An entitlement with no linked campaign stays unfulfilled indefinitely, which is recorded as an open owner decision rather than automated, because automating it would mean the system approving its own spending.
- WPS-021 retires two dormant day-one scaffolds from `202607200002`. `public.promo_codes` carried a policy granting **every authenticated account** `select` on every active code, and `usage_limit` was a column nothing enforced. `public.wallets` carried a `balance_egp` column. Both, and their companion tables, are now RLS-enabled with **no policy**, stripped of every client grant, and commented as retired. They are deliberately **not dropped**: they exist on the hosted project and `promo_code_uses` carries foreign keys into `customer_profiles` and `bookings`, so a drop is irreversible while retiring loses nothing.
- The `public.wallets` scaffold was **missed by the first pass of the WPS-021 architecture audit**, which searched the client source for wallet usage rather than the schema and reported that no wallet system existed. A pgTAP assertion failed with `have: 2` and was correct. The audit was corrected in place and the finding is recorded in `docs/decisions/promotion-scope-and-legacy-scaffolds.md`.
- WPS-021 found and fixed a real financial defect during implementation: writing only `price_breakdown.discount` left the customer paying full price and pushed `provider_gross_minor` to 105,000 on a 100,000 booking, making Warsha fund a worker bonus rather than a customer discount. WPS-007 treats `final_price_egp` as the customer-facing total, so redemption now reduces it as well. Caught by a pgTAP assertion on worker gross and asserted permanently.
- WPS-021 enables nothing. Both growth feature flags ship `enabled=false, audience='none'`, the growth kill switch ships inactive, and every campaign starts as a draft that a second staff member must approve through WPS-018 dual control. Applying `202608060001` to a hosted project changes no customer behaviour until somebody deliberately turns a flag on.
- **WPS-021 v1.1 corrected the referral model.** v1.0 coupled a referral reward to a later admin campaign through an inert entitlement, which put a human in the loop *per referral*: somebody whose friend completed a real job held something labelled "earned" that paid nothing until an unrelated administrative act. v1.1 moves the human decision to approving the **programme**, once, in advance, and the server then grants rewards **automatically** on qualification. `referral_reward_entitlements`, `campaign_eligibility_grants`, and `growth_campaigns.requires_grant` are gone, and pgTAP asserts all three no longer exist so the coupling cannot silently return.
- WPS-021 v1.1 makes referrals and admin promotions **fully independent**: two evaluators that read none of each other's tables, two feature flags, two kill switches, two capability pairs, and two audit trails. An account that never referred anybody can receive a promotion; an account that referred somebody receives its reward with no campaign in existence. Campaign eligibility uses only stated facts — completed booking count, account age, inactivity, city, category — with no behavioural score, inferred value, or paid ranking, asserted by a negative test.
- WPS-021 v1.1 bounds programme budgets by reserving each reward's **ceiling at grant time**, not at redemption. Reserving at redemption would let a programme grant unlimited rewards against a small budget because nothing had been spent yet; reserving the ceiling makes the budget bound outstanding liability, and the remainder is released on redemption for less, on expiry, and on cancellation.
- WPS-021 stacking is a constraint, not a convention: `unique (booking_id)` on `public.booking_benefit_redemptions` plus a check that exactly one benefit source is named. At most one referral reward **or** one admin promotion per booking, never both.
- `202608060001` was corrected **in place** rather than by a follow-on migration. It was confirmed absent from the remote ledger (`{"local":"202608060001","remote":""}`), is unpushed and uncommitted, and no remotely applied migration was edited.
- WPS-021 v1.1 passes a clean reset and **24 pgTAP files / 2,329 assertions (`Result: PASS`)**, including a 159-assertion WPS-021 suite; all 23 pre-existing suites pass with no assertion edited. 21 of 22 regression suites pass (363 WPS-021 client checks) with one pre-existing `app.json` failure WPS-021 did not cause; five audit scripts, TypeScript, ESLint, mojibake, Expo Doctor 18/18, and cache-cleared Android/iOS/Web exports pass. The 68-case manual alpha remains **NOT RUN**.
- WPS-021's first pgTAP execution failed on a **test fixture**, not on product code: `reset role` restores the session role but not the JWT claims, which `pg_temp.act_as` sets with transaction scope, so a booking fixture inherited the staff identity that had just activated a referral programme and `private.record_booking_status()` correctly refused a booking created on somebody else's behalf. Two functions raise the identical message `Booking action is not available` — `record_booking_status` on the actor check (`42501`) and `enforce_booking_transition` on an illegal edge (`22023`) — which is what makes the actor fault read like a transition fault. The correction was one `act_as` / `act_as_nobody` pair in the fixture; no authority, policy, grant, or lifecycle rule was changed.
- The pending hosted chain after WPS-021 is **one** migration: `202608060001` (WPS-021). The linked dry run confirmed exactly that one, without mutating the hosted project. The documented command `npx.cmd supabase db push --linked` was **not executed**.
- WPS-022 found a **public, world-readable, unowned storage bucket**. `avatars` shipped on day one with `public = true`, no size limit, no MIME allowlist, and a policy letting any authenticated account write into a folder named after its own uid. Nothing had referenced it since WPS-010 introduced `profile-images`. It was retired **in place** — made private, bounded, and stripped of its write policy so `storage.objects` is deny-by-default for it — rather than dropped, because dropping a bucket is irreversible and hosted objects may exist that a local migration cannot see. This is the same treatment WPS-021 gave `promo_codes` and `wallets`.
- WPS-022 also found that `anon` and `authenticated` held leftover `TRUNCATE`, `REFERENCES` and `TRIGGER` grants on **every** table in `public`, from Supabase's bootstrap `grant all`. Earlier hardening revoked the DML verbs and left these behind. None is reachable through PostgREST, but **`TRUNCATE` bypasses RLS entirely**, so holding it was a standing defect waiting for a connection string to leak. All three are revoked and asserted absent.
- WPS-022 invents **no statutory retention period**. Ten of eleven retention rules carry `legal_review_status = 'pending'`, sixteen open legal questions are recorded in `docs/privacy/WARSHA-PRIVACY-LEGAL-QUESTIONS.md`, and a five-condition guard means no rule is executable as shipped. The client suite asserts that no user-facing string contains `complian`, `legally required`, `by law`, `regulat`, or `statutor`.
- WPS-022 describes its deletion outcome as **pseudonymization, never anonymity**. The account UUID survives because it is the join key under a worker's payout and a customer's receipt, and anything linkable to an account is personal data. The deletion screen states what remains, and that processing is neither instant nor total, **before** a request is made rather than after.
- WPS-022 keeps four gaps honestly disabled rather than half-built: no worker produces an export **file** (the request stops at `manifest_ready` and the copy says "being prepared"), no scheduler advances a deletion past `cooling_off`, anonymization clears rows but not storage objects, and sign-in disablement is an auth-layer operation WPS-022 does not own. All four are recorded in WES-022 §9 and in the acceptance evidence.
- The pending hosted chain after WPS-022 is **two** migrations: `202608060001` (WPS-021) and `202608070001` (WPS-022). Both are local-only. The documented command `npx.cmd supabase db push --linked` was **not executed**.
- **Superseded during WPS-023.** An executed `supabase migration list --linked` on 2026-08-06 shows both of those now applied remotely; the hosted project is current through `202608070001`. The statement above is retained as the record of what was true when WPS-022 closed.
- WPS-023 found that the application **had no authentication gate at all**. `AuthProvider` resolved a session and put it in context; nothing branched on the result. The app opened onto the customer home while signed out, and the only authentication surface in the product was the Profile tab, which rendered a sign-in form instead of a profile. Everything else was saved from the consequences by RLS underneath. The fix is `components/warsha/AuthGate.tsx`, which renders nothing operational until the session and the onboarding state are both known — deliberately not a skeleton, because a loading screen that impersonates the signed-in app is a loading screen that lies about whether you are signed in.
- WPS-023 found **fifteen `public` functions executable by `anon`**, including `staff_create_legal_hold`, by querying the running database rather than reading the migration source. WPS-022 had written `revoke all on function ... from anon` for every one of them, and every one was a no-op: `anon` never held a direct grant, it inherited `EXECUTE` from the default `PUBLIC` grant, and **`REVOKE ... FROM anon` cannot remove what `anon` inherits from `PUBLIC`**. All fifteen fail closed at runtime, so this was never exploitable alone; it was still a real defect. WPS-022's own suite asserted the *behaviour* (`throws_ok`) and so could not see it. WPS-023 asserts the *privilege*, and bounds the surface as a property: exactly nine allowlisted sanitized reads, whatever gets added later. Measured 24 to 9.
- WPS-023's largest design decision was to **not** add its gate to `private.is_provider_publicly_discoverable`. That is the tidiest enforcement point — discovery, search, catalog and quote invitations all flow through it — and it broke **fifty-two assertions across nine existing suites**. Those failures were WPS-006, WPS-010, WPS-011 and WPS-020 stating what "discoverable" means. The predicate was restored unchanged and the gate moved to where a worker actually acts: `is_published` is set only by a gated activation decision, and worker verbs call `private.require_active_worker()`. A worker who has not been activated is never published, never discoverable, and refused at every worker verb — without changing a definition WPS-023 does not own.
- WPS-023 backed out of two further inventories it did not own. Two `staff_queues` rows broke WPS-017's assertion of exactly eighteen queues, so **no queue row is added** and reviewers reach their work through a capability-checked RPC instead. Three `observability_retention_policy` rows broke WPS-018's invariant that no declared log stream contains personal data — vetting history and staff evidence are decision records about a named person, not logs, and registering them there would have forced either a lie in `contains_personal_data` or the loss of the invariant. They are registered in `private.data_inventory`, the register WPS-022 built for exactly this.
- WPS-023 does **not** implement "any offence within the previous twelve months automatically rejects the worker". That rule cannot distinguish an accusation from a final conviction, a mistaken identity from a real one, or an offence relevant to entering somebody's home from one that is not. What exists is a versioned policy whose twelve factors are all `reviewer_judgement`, whose `legal_review_status` is `pending`, and which no code path reads to produce an outcome. Fifteen open legal questions are recorded in WPS-023 §18. Model A is stated plainly on the certificate screen in both languages: the worker obtains the certificate themselves, and **Warsha has no Ministry integration, no government API, and no way to confirm authenticity**.
- A WPS-023 regression check searched the migration for `automatic reject` and **failed** — because the seeded policy `notes` say "No automatic rejection rule is implemented", and that sentence satisfied the check for the thing it described. This is the same failure mode `codeOf()` prevents for comments, one level out: **a string asserting an absence is not evidence of that absence**. It was replaced with checks for the machinery such a rule would need — no interval arithmetic in the vetting path, no outcome derived from document recency, and every write of `'rejected'` inside a function that opens with `require_staff_capability` and refuses without recorded evidence.
- WPS-023 grandfathers every pre-existing provider into `manual_review`, **never `active`**. None has been through WPS-023 identity confirmation or submitted a certificate, and silently marking them active would be exactly the automatic approval the specification forbids. There is no bulk reactivation, and there should not be — a bulk activation is an automatic approval wearing a different name.
- WPS-023's implementing session was interrupted by the session limit with all twenty-four code artefacts complete and **none of the eighteen documents written**. The recovering session verified every artefact against the repository rather than against a summary, wrote the documentation set, and re-ran every gate from the completed tree. `audit:environment` caught a real defect during that pass: `app/admin/vetting.tsx` bypassed the WPS-017 guarded admin shell. It was rewritten onto `AdminShell` and `useAdmin`.
- The pending hosted chain after WPS-023 is **one** migration: `202608080001` (WPS-023). Local and hosted are otherwise in sync at 37 applied migrations. The linked dry run confirmed exactly that one without mutating the hosted project. The documented command `npx.cmd supabase db push --linked` was **not executed**.
- WPS-024 found that Warsha had consent but no **agreements**. WPS-022’s `privacy_consent_purposes` already carried a `document_key` and a `current_version` — the right seam — with no documents behind it, and two of those keys pointed at documents that did not exist. WPS-024 supplies twenty-six and extends that model rather than building a second one: `record_my_consent` is untouched, and `accept_legal_document` moves the matching purpose forward so the privacy centre and the legal centre cannot disagree.
- **The text lives in the repository and the hash lives in the database.** Thirty thousand words of legal prose inside a migration would be unreviewable, would diff uselessly, and would make correcting a comma a database migration. The client sends the hash of what *it* rendered, so a stale bundle is refused loudly rather than recording an acceptance of a version the person never saw. Three independent SHA-256 implementations — TypeScript, Node, Postgres — are pinned to each other by assertions in both suites.
- WPS-024’s own SHA-256 was **wrong for one input length in sixty-four**. It computed padded length as `((len + 9) >> 6) + 1` blocks, which is the ceiling except when `len + 9` is an exact multiple of 64 — where it appends an empty block and changes the digest. `"abc"` hashed correctly. A thousand `a`s hashed correctly. Twenty-five of the twenty-six legal documents hashed correctly. `version_history` did not. Only an exhaustive sweep across every input length from 0 to 320 found it, and that sweep is now a permanent check. A wrong hash in the register would have made the acceptance chain provably wrong for one document — discovered only when somebody tried to prove what a person had agreed to.
- `private.legal_current_version` originally returned a composite type. A composite-returning SQL function yields **one row of NULLs** when nothing matches, not zero rows, so every downstream `join lateral ... on true` produced a phantom obligation with a NULL version that read as outstanding and that nobody could ever satisfy. It returns `setof` now. The defect was masked until the corpus dates were corrected: obligation counts were *right by accident* while every version was ineffective.
- The WPS-024 pgTAP suite initially ran as a superuser after its fixture block, so RLS was bypassed and the isolation assertions — *one account cannot read another’s acceptances*, *a client cannot insert its own acceptance* — **passed by not being tested**. That is the worst way for a security test to succeed. The suite now runs its client-behaviour block as `authenticated`.
- WPS-024 backed out of WPS-023’s signed-out surface rather than widening it. The first draft granted `get_legal_document_register()` to `anon`, reasoning that a person must be able to read the terms before creating an account. They can — the corpus is bundled, so the reader renders full text from the device with no call at all. What `anon` would have gained was version metadata that only matters to accounts. **The nine sanctioned signed-out reads stay nine.**
- Three new `public` tables silently inherited `TRUNCATE`, `REFERENCES` and `TRIGGER` from Supabase’s default privileges; `grant select` is additive and left all of it in place. WPS-022’s property assertion over every public table caught it. That is the second consecutive specification in which a *property* assertion caught something no behavioural test could see.
- **Training on identity documents is barred by a CHECK constraint, not a policy note.** `check (not (covers_identity_data and permitted_for_training))` means flipping it is a migration that must be written, reviewed and deployed — and that friction is exactly the governance the AI Usage Policy promises. The pgTAP suite asserts it by attempting the update and requiring `23514`.
- No document states a commission percentage, a cancellation fee or a payout period, because `private.payment_configuration` ships with a null commission and a disabled gateway. The agreements bind the **mechanism** — the rate is displayed before a worker accepts a job, changing it is a material change, and it never applies retroactively. A figure in a binding document that no system enforces is worse than a mechanism that binds, and the regression suite asserts the absence of both a percentage and a monetary amount across the whole corpus.
- **Every lawful basis in the data-processing register is recorded as proposed and pending legal review. Not one is approved.** Egyptian data protection law and its executive regulations continue to develop and no advice has been obtained. Asserting a settled characterisation would be a compliance claim dressed as a description, and a reader could not tell the difference. The pgTAP suite asserts that no activity is recorded as approved, so the honest state cannot drift into a confident one.
- Twelve of the twenty-six documents carry a **full parallel Arabic text** and fourteen carry a complete Arabic summary with English named authoritative. The line is drawn at what a person is asked to *agree* to: all four acceptance-required documents have full Arabic. Machine-quality legal Arabic could create obligations that differ from the English, which is worse than a clearly-labelled summary — and the label is on the page, not in a footnote. Full translation of the remaining fourteen is a legal-review item.
- WPS-024 moves worker activation from post-review to **provisional activation with post-hoc review**, which is a real reduction in assurance and a deliberate product trade. What it does not do is give the system any power to make a decision: `system` can reach `provisionally_active` and still cannot reach `active`, `approved`, `rejected` or `suspended`. Every WPS-023 state-machine assertion passes unedited. The provisional gate set is derived **by subtraction** from the full set, so a gate added to WPS-023 later protects both tiers rather than only the slower one.
- `audit:bundle` reports a Supabase secret in the Android and iOS Hermes bundles and **there is no secret**. Hermes packs string literals contiguously, so `@supabase/functions-js`’s own guard literal runs into unrelated neighbours; the web bundle of identical source is clean across 70 artefacts and shows the literal quoted and terminated. The script documents this failure mode itself. It was **not modified**: entropy was measured as a replacement discriminator and rejected because identifier runs reach 4.73 bits per character while random keys fall to 4.62, so any threshold would produce false negatives on real keys.
- The pending hosted chain after WPS-024 is **one** migration: `202608090001` (WPS-024). Local and hosted are otherwise in sync at 38 applied migrations. The linked dry run confirmed exactly that one without mutating the hosted project. The documented command `npx.cmd supabase db push --linked` was **not executed**.
- WPS-024 activates the provider layer: Expo Camera with a framing overlay in the aspect ratio of an Egyptian National ID, Image Picker and Document Picker as the guaranteed fallbacks, private Supabase Storage, the staff vetting decision surface WPS-023 deliberately shipped read-only, and Google Cloud Vision and Google Maps Platform behind server-side Edge Functions. **Neither Google service is enabled**, because no credential has been supplied to this environment, and `private.external_providers` records both as `implemented_awaiting_credential` rather than as active. A register that describes what is built rather than what is running is a wish.
- The **Maps key is two keys with different natures**, and conflating them is how this integration would leak money. The RENDER key must reach the device — the native SDK reads it from the app manifest and there is no server-side way to draw a map on a phone — so it is publishable, restricted by package name and signing fingerprint, and scoped to rendering only. Everything billed per request goes through `location-proxy`, which holds a separate genuinely secret key that never leaves the backend. A client calling Places directly would need the billed key on the device, and anyone could then spend Warsha’s Maps budget.
- The **External Provider Registry is deliberately not the Subprocessor Register**. Expo Camera is the clarifying case: it is an external dependency Warsha must track, version and be able to disable, so it belongs in the operational register; it runs entirely on the device and sends Warsha’s data to nobody, so it is not a subprocessor and listing it in a published privacy disclosure would inflate that disclosure with something that receives nothing. `staff_sync_provider_status()` keeps the two in step and refuses to promote a supplier whose flag is still off.
- **The OCR accuracy baseline is NOT MEASURED, and the harness refuses to invent one.** Measuring it needs a Google Cloud Vision credential and a set of Egyptian National ID samples with transcribed ground truth; neither exists in this environment. The harness exits non-zero and writes nothing without both, and refuses a sample set in which every sample is marked readable — an unreadable rate cannot be measured from clean images, and a baseline built only from good photographs measures the wrong thing. A fabricated baseline would be worse than an absent one: it is the number every future change is judged against, and the fiction would be indistinguishable from a measurement to everyone who read it later.
- Behavioural tests of the National ID parser found a real defect before any provider was ever called. The Arabic-name extractor matched across line boundaries because `\s` includes a newline, so a card reading «البطاقة الشخصية» above «محمد أحمد إبراهيم» produced the legal name *"Personal ID Card Mohamed Ahmed Ibrahim"*. A worker skimming a pre-filled form would very plausibly have accepted it — precisely the confident-and-wrong outcome the accuracy baseline measures separately from accuracy, and precisely why it is measured separately.
- `expo-image-manipulator` ships **no config plugin**, and listing it in `app.json` made `expo config` exit non-zero, which failed Expo Doctor with no error output at all. The dependency needs no native configuration; the plugin entry was removed and Doctor returned to 18/18. Worth recording because the failure mode — a silent non-zero exit from a config resolver — gives a reader nothing to search for.
- Edge Functions are Deno, not app TypeScript, and pulling them into `tsc` produced errors about a runtime that is not the app’s. The entry points are excluded from the app tsconfig; `provider-secrets.ts` stays in scope and declares the single Deno global it touches **module-locally**, rather than an ambient `Deno` namespace being introduced globally where it would mask a real error in app code that referenced Deno by mistake.
- The pending hosted chain after provider activation is **two** migrations: `202608090001` (governance) and `202608100001` (provider activation). Local and hosted are otherwise in sync at 38 applied migrations. The linked dry run confirmed exactly those two without mutating the hosted project. The documented command `npx.cmd supabase db push --linked` was **not executed**.
- WPS-024's final pass **removed both vendors from business logic**. `get_extraction_capability()` asked `provider_enabled('google_cloud_vision')` and `get_location_capability()` asked about `google_maps_platform`; building a TypeScript provider interface while the database still named the vendor would have moved the coupling rather than removed it, because you could write a second OCR implementation and still not switch to it without editing a migration. The registry now records a **capability role** — what a provider is FOR — and everything downstream asks for the role. Exactly five files may name a vendor, and the suites assert that no other does.
- `identity_ocr` and `location` are **singular roles**, enforced by a partial unique index rather than by a function: two active OCR providers would make "which one read this document" unanswerable and the audit trail worthless. `document_capture` is plural on purpose, because camera, image picker and document picker all fill it and a worker uses whichever their device and their document allow.
- `renderMap()` is on the map interface and returns a **descriptor, not an image**, because no server can draw a map on a phone. It names a renderer the client resolves to a component. Without it, swapping map vendor would mean editing the address component, and that component would go on importing `react-native-maps` for a vendor no longer in use. The renderer key is declared in two runtimes that cannot import each other, so the regression suite reads both files and asserts they agree and pgTAP asserts the registry holds the same string.
- Provider health tracks latency, timeouts, failures, retries, availability, last success and version — and holds **no account, no document, no extracted value and no credential**, asserted as a property of the schema. An operations screen that could also answer "what did this worker submit" would be a second route to identity data behind a different capability, and the first thing anyone would do with it is look somebody up. Two smaller decisions follow the same logic: **availability excludes Warsha's own kill switch**, because counting our decision against a supplier makes the figure meaningless during exactly the incident it exists for; and **an unobserved window reports null, not 100%**, because a provider nobody has called since Tuesday is not healthy.
- Extracting the provider interface found **two defects that would have broken extraction for every user**, neither visible to a test that reads source text. `btoa(String.fromCharCode(...bytes))` spreads every byte as a function argument and throws `RangeError` above roughly 100,000 of them, so it would have failed on every real photograph while passing every small fixture. And `private.worker_identity_extractions` had no `document_type`, `document_hash` or `is_current` columns, so every successful extraction would have failed at the insert — provider called, audit row written, candidates on screen, nothing persisted for the worker to confirm. `is_current` is the one with a safety consequence: without supersession a worker could confirm a field extracted from a photograph they had already discarded.
- The OCR harness now distinguishes **absence from invalidity**. No sample set and no endpoint is not a fault — the measurement simply has not been made, so it records NOT MEASURED and exits 0 rather than holding a pipeline red for months. A forbidden sample source, a missing image, a manifest of only clean photographs or a readable sample with no transcribed ground truth *would* produce a misleading figure, so those exit non-zero. Both refuse to invent anything. It also measures **parser failures separately from OCR failures**, because a card the provider read perfectly from which Warsha's own regular expressions extracted nothing is not a vendor problem, and merging the two would send the next person to argue with a supplier about a parser.
- `audit:secrets` scans `git ls-files` — **tracked files only** — and all of WPS-024 is uncommitted, so its fifty-nine new files were outside that scan entirely. They were swept separately for the same credential shapes plus `"type":"service_account"` and `"private_key":`, with no findings. Worth recording because "the secret scan is clean" against work that is 100% untracked is a guarantee that does not cover the work.
- The Hermes bundle finding **went green for a reason unrelated to security**. The `sb_secret_` guard literal is still packed contiguously in the string table; the discriminator requires a digit in the identifier run that follows it, and this build's code changes moved the digit-bearing neighbours elsewhere. Nothing was fixed and nothing was weakened — the coincidence simply moved, and it can move back on any build that reorders string literals. Recorded so the next person to see it fire does not re-derive the analysis.
- The pending hosted chain after the final pass is **three** migrations: `202608090001` (governance), `202608100001` (provider activation) and `202608110001` (provider abstraction and health). Local and hosted are otherwise in sync at 38 applied migrations. The linked dry run confirmed exactly those three without mutating the hosted project. The documented command `npx.cmd supabase db push --linked` was **not executed**.
