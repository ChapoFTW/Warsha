import MaterialIcons from '@expo/vector-icons/MaterialIcons'; import { router } from 'expo-router'; import { Pressable,StyleSheet } from 'react-native'; import { colors,radii,spacing,typography } from '@/constants/theme'; import { Category } from '@/src/data/mock-data'; import { useLocalization } from '@/src/i18n/localization'; import { AppText } from './Typography';
export function CategoryCard({item}:{item:Category}){const{t}=useLocalization();return <Pressable accessibilityRole="button" onPress={()=>router.push({pathname:'/categories/[id]',params:{id:item.id}})} style={({pressed})=>[styles.card,pressed&&{opacity:.7}]}><MaterialIcons name={item.icon} size={30} color={colors.textPrimary}/><AppText numberOfLines={1} style={styles.label}>{t(item.label)}</AppText></Pressable>}
const styles=StyleSheet.create({card:{width:96,height:101,borderRadius:radii.lg,backgroundColor:colors.surfaceElevated,borderWidth:1,borderColor:colors.border,alignItems:'center',justifyContent:'center',gap:spacing.sm},label:{fontSize:12,fontWeight:typography.medium,textAlign:'center'}});
