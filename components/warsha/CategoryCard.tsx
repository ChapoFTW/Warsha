import MaterialIcons from '@expo/vector-icons/MaterialIcons'; import { router } from 'expo-router'; import { Pressable,StyleSheet } from 'react-native'; import { radii, spacing, typography, type ThemeColors } from '@/constants/theme'; import { useThemeColors, useThemedStyles } from '@/src/appearance/appearance-context'; import { Category } from '@/src/data/mock-data'; import { useLocalization } from '@/src/i18n/localization'; import { AppText } from './Typography';
export function CategoryCard({item}:{item:Category}){
  const colors = useThemeColors();
  const styles = useThemedStyles(makeStyles);const{t}=useLocalization();return <Pressable accessibilityRole="button" onPress={()=>router.push({pathname:'/categories/[id]',params:{id:item.id}})} style={({pressed})=>[styles.card,pressed&&{opacity:.7}]}><MaterialIcons name={item.icon} size={27} color={colors.textPrimary}/><AppText numberOfLines={1} style={styles.label}>{t(item.label)}</AppText></Pressable>}
const makeStyles = (colors: ThemeColors) => StyleSheet.create({card:{width:96,height:104,borderRadius:radii.lg,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.borderSoft,alignItems:'center',justifyContent:'center',gap:spacing.sm},label:{fontSize:12,fontWeight:typography.medium,textAlign:'center'}});

