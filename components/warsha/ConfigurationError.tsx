import { StyleSheet,View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { BrandLogo } from './BrandLogo'; import { GlobalPreferenceControls } from './GlobalPreferenceControls'; import { AppText } from './Typography'; import { spacing, typography, type ThemeColors } from '@/constants/theme'; import { useThemedStyles } from '@/src/appearance/appearance-context'; import { useLocalization } from '@/src/i18n/localization';
export function ConfigurationError(){
  const s = useThemedStyles(makeS);const{t}=useLocalization();return <SafeAreaView style={s.safe}><GlobalPreferenceControls/><View style={s.content}><BrandLogo size={64}/><AppText style={s.title}>{t('configurationRequired')}</AppText><AppText style={s.body}>{t('configurationInstructions')}</AppText></View></SafeAreaView>}
const makeS = (colors: ThemeColors) => StyleSheet.create({safe:{flex:1,backgroundColor:colors.background},content:{flex:1,maxWidth:520,width:'100%',alignSelf:'center',alignItems:'center',justifyContent:'center',padding:spacing.xxl,gap:spacing.lg},title:{fontSize:22,fontWeight:typography.semibold,textAlign:'center'},body:{fontSize:13,lineHeight:20,color:colors.textSecondary,textAlign:'center'}});
