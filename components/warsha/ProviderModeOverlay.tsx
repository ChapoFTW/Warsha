import { router,usePathname } from 'expo-router';import { Pressable,StyleSheet } from 'react-native';import { AppText } from './Typography';import { colors,radii,spacing } from '@/constants/theme';import { useProviderText } from '@/src/i18n/provider-translations';import { useProviderFoundation } from '@/src/providers/provider-context';
export function ProviderModeOverlay(){const pathname=usePathname();const pt=useProviderText();const{setMode}=useProviderFoundation();if(pathname!=='/provider-mode')return null;return <Pressable accessibilityLabel={pt('customerMode')} onPress={()=>void setMode('customer').then(()=>router.replace('/'))} style={styles.button}><AppText style={styles.text}>{pt('customerMode')}</AppText></Pressable>};const styles=StyleSheet.create({button:{position:'absolute',right:spacing.lg,top:spacing.lg,zIndex:20,minHeight:42,paddingHorizontal:spacing.md,borderRadius:radii.pill,borderWidth:1,borderColor:colors.border,backgroundColor:colors.surfaceElevated,alignItems:'center',justifyContent:'center'},text:{fontSize:11}});
