import { MaterialIcons } from '@expo/vector-icons';import { router,usePathname } from 'expo-router';import { Pressable,StyleSheet,View } from 'react-native';import { AppText } from './Typography';import { colors,radii,spacing } from '@/constants/theme';import { useProviderText } from '@/src/i18n/provider-translations';import { useNotificationText } from '@/src/notifications/notification-translations';import { useNotifications } from '@/src/notifications/notification-context';import { useProviderFoundation } from '@/src/providers/provider-context';
export function ProviderModeOverlay(){const pathname=usePathname();const pt=useProviderText();const nt=useNotificationText();const{unreadCount}=useNotifications();const{setMode}=useProviderFoundation();if(pathname!=='/provider-mode')return null;return <View style={styles.container}><Pressable accessibilityRole="button" accessibilityLabel={nt('notificationBell')} onPress={()=>router.push('/notifications')} style={styles.iconButton}><MaterialIcons name="notifications-none" size={21} color={colors.textPrimary}/>{unreadCount?<View style={styles.badge}><AppText style={styles.badgeText}>{unreadCount>9?'9+':unreadCount}</AppText></View>:null}</Pressable><Pressable accessibilityRole="button" accessibilityLabel={pt('customerMode')} onPress={()=>void setMode('customer').then(()=>router.replace('/'))} style={styles.button}><AppText style={styles.text}>{pt('customerMode')}</AppText></Pressable></View>};const styles=StyleSheet.create({container:{position:'absolute',right:spacing.lg,top:spacing.lg,zIndex:20,flexDirection:'row',gap:spacing.sm},button:{minHeight:42,paddingHorizontal:spacing.md,borderRadius:radii.pill,borderWidth:1,borderColor:colors.border,backgroundColor:colors.surfaceElevated,alignItems:'center',justifyContent:'center'},iconButton:{width:42,height:42,borderRadius:radii.pill,borderWidth:1,borderColor:colors.border,backgroundColor:colors.surfaceElevated,alignItems:'center',justifyContent:'center'},badge:{position:'absolute',right:-4,top:-5,minWidth:18,height:18,paddingHorizontal:4,borderRadius:9,backgroundColor:colors.white,alignItems:'center',justifyContent:'center'},badgeText:{color:colors.background,fontSize:9,fontWeight:'800'},text:{fontSize:11}});
