import MaterialIcons from '@expo/vector-icons/MaterialIcons'; import { router } from 'expo-router'; import { useState } from 'react'; import { Pressable,StyleSheet, TextInput, View } from 'react-native'; import { colors,spacing,typography } from '@/constants/theme'; import { useLocalization } from '@/src/i18n/localization';
export function SearchBar(){const{t,isRTL}=useLocalization();const[query,setQuery]=useState('');const submit=()=>router.push({pathname:'/search',params:query.trim()?{q:query.trim()}:undefined});return <View style={[styles.box,isRTL&&styles.reverse]}><MaterialIcons name="search" size={26} color={colors.textPrimary}/><TextInput accessibilityLabel={t('searchProviders')} value={query} onChangeText={setQuery} onSubmitEditing={submit} returnKeyType="search" placeholder={t('searchPlaceholder')} placeholderTextColor={colors.textMuted} style={[styles.input,{textAlign:isRTL?'right':'left',writingDirection:isRTL?'rtl':'ltr'}]}/><View style={styles.divider}/><Pressable onPress={submit} hitSlop={10}><MaterialIcons name="tune" size={22} color={colors.textPrimary}/></Pressable></View>}
const styles=StyleSheet.create({box:{height:58,borderRadius:22,backgroundColor:colors.surfaceElevated,borderWidth:1,borderColor:colors.border,flexDirection:'row',alignItems:'center',paddingHorizontal:18,gap:spacing.md},reverse:{flexDirection:'row-reverse'},input:{flex:1,color:colors.textPrimary,fontSize:16,fontFamily:typography.family,paddingVertical:0},divider:{height:24,width:1,backgroundColor:colors.border}});
