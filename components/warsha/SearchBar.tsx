import MaterialIcons from '@expo/vector-icons/MaterialIcons'; import { router } from 'expo-router'; import { useState } from 'react'; import { Pressable,StyleSheet,TextInput,View } from 'react-native'; import { brandFontFamily,colors,radii,spacing } from '@/constants/theme'; import { useLocalization } from '@/src/i18n/localization';
export function SearchBar(){const{t,isRTL}=useLocalization();const[query,setQuery]=useState('');const submit=()=>router.push({pathname:'/search',params:query.trim()?{q:query.trim()}:undefined});return <View style={[styles.box,isRTL&&styles.reverse]}><MaterialIcons name="search" size={24} color={colors.textSecondary}/><TextInput accessibilityLabel={t('searchProviders')} value={query} onChangeText={setQuery} onSubmitEditing={submit} returnKeyType="search" placeholder={t('searchPlaceholder')} placeholderTextColor={colors.textMuted} style={[styles.input,{fontFamily:brandFontFamily(isRTL),textAlign:isRTL?'right':'left'}]}/><View style={styles.divider}/><Pressable accessibilityRole="button" accessibilityLabel={t('openFilters')} onPress={()=>router.push({pathname:'/search',params:{filters:'1'}})} hitSlop={10}><MaterialIcons name="tune" size={22} color={colors.textPrimary}/></Pressable></View>}
const styles=StyleSheet.create({box:{height:58,borderRadius:radii.md,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border,flexDirection:'row',alignItems:'center',paddingHorizontal:spacing.lg,gap:spacing.md},reverse:{flexDirection:'row-reverse'},input:{flex:1,color:colors.textPrimary,fontSize:15,paddingVertical:0},divider:{height:24,width:1,backgroundColor:colors.border}});
