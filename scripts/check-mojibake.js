const fs=require('fs');const path=require('path');const roots=['app','components','constants','src'];const extensions=new Set(['.ts','.tsx','.js','.json','.md']);const suspicious=/Ãƒ|Ã‚|Ã¢|ï¿½|â˜|â€¦|â€“|â€™|â€œ|â€|â†|Â·/u;const failures=[];function scan(dir){for(const entry of fs.readdirSync(dir,{withFileTypes:true})){const file=path.join(dir,entry.name);if(entry.isDirectory())scan(file);else if(extensions.has(path.extname(file))){const text=fs.readFileSync(file,'utf8');text.split(/\r?\n/).forEach((line,index)=>{if(suspicious.test(line))failures.push(`${file}:${index+1}`)})}}}roots.filter(fs.existsSync).forEach(scan);if(failures.length){console.error(`Likely mojibake found:\n${failures.join('\n')}`);process.exit(1)}console.log('No likely mojibake found.');

