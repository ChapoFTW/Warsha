import { getAdminClient } from './supabase-admin-client.mjs';
const email=process.env.DEV_AUTH_EMAIL;const password=process.env.DEV_AUTH_PASSWORD;const displayName=process.env.DEV_AUTH_NAME??'Warsha Test Customer';const role=process.env.DEV_AUTH_ROLE==='provider'?'provider':'customer';if(!email||!password)throw new Error('Set DEV_AUTH_EMAIL and DEV_AUTH_PASSWORD in this terminal.');const{data,error}=await getAdminClient().auth.admin.createUser({email,password,email_confirm:true,user_metadata:{display_name:displayName,preferred_language:'en',account_role:role,terms_accepted_at:new Date().toISOString(),privacy_accepted_at:new Date().toISOString()}});if(error)throw error;console.log(`Created development Auth user ${data.user.id}.`);
